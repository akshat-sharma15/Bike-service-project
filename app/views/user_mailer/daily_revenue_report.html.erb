<h1>Daily Revenue Report</h1>

<p>Hello <%= service_center.service_owner.name %>,</p>

<p>Here is the revenue report for <%= Date.today.strftime('%B %d, %Y') %>:</p>

<p>Total Revenue: <%= number_to_currency(service_center.revenue) %></p>

<h2>Completed Slots:</h2>
<table>
  <thead>
    <tr>
      <th>Slot ID</th>
      <th>Status</th>
      <th>Booking Date</th>
      <th>Service Type</th>
    </tr>
  </thead>
  <tbody>
    <% service_center.slots.each do |slot| %>
      <tr>
        <td><%= slot.id %></td>
        <td><%= slot.status %></td>
        <td><%= slot.booking_date %></td>
        <td><%= slot.service_type %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<p>Thank you,</p>
<p>Your Service Center</p>
