<h1>Daily Revenue Report</h1>
<p>Hello <%= @service_center.service_owner.name %>,</p>
<p>Here is the revenue report for <%= Date.today.strftime('%b %d, %y') %>:</p>
<p>Total Revenue: <%= number_to_currency(@service_center.revenues.find_by(date: Date.today)&.revenue) %></p>

<h2> Slots:</h2>
<table>
  <thead>
    <tr>
      <th>Slot ID</th>
      <th>Status</th>
      <th>Booking Date</th>
      <th>Service Type</th>
      <th>Customer Name</th>
    </tr>
  </thead>
  <tbody>

    <%slots = @service_center.slots.where(booking_date: Date.today)%>
    <% slots.each do |slot| %>
      <tr>
        <td><%= slot.id %></td>
        <td><%= slot.status %></td>
        <td><%= slot.booking_date %></td>
        <td><%= slot.service_type %></td>
        <td><%= slot.client_user.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<p>Thank you,</p>
<p>Your Service Center</p>
